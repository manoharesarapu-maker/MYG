# Anthropic API key used for semantic classification, question phrasing, and extraction.
# Never commit a real key. Read at runtime via process.env.ANTHROPIC_API_KEY.
ANTHROPIC_API_KEY=

# Model used for all Claude calls in this demo.
ANTHROPIC_MODEL=claude-sonnet-4-6
